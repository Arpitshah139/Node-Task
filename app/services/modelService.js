const clientModel = require('../models/clientModel').clientModel;
const menu = require('../models/menuModel').menu;
const submenu = require('../models/subMenuModel').submenu;


module.exports = {
    clientModel: clientModel,
    menu: menu,
    submenu: submenu
};